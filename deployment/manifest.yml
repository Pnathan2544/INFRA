# =============================================================
# Deployment Manifest
# =============================================================
# This file represents the production deployment candidate created by CI/CD.
# The manifest uses the source commit SHA as the conceptual Docker image tag,
# giving a traceable chain: commit SHA -> image tag -> manifest -> runtime.
# =============================================================

apiVersion: v1
kind: deployment
metadata:
  name: agent-api
  environment: production
  commit_sha: "7493f410a8f2e6716b2e995b4713b939a378e22b"
  short_sha: "7493f41"
  deployed_by: "Pnathan2544"
  deployed_at: "2026-06-08T18:23:01Z"
  workflow_run_id: "27158121096"
  workflow_run_number: "2"
  source_branch: "main"

spec:
  replicas: 2

  container:
    name: agent-api
    image_tag: "7493f410a8f2e6716b2e995b4713b939a378e22b"
    port: 8080

    env:
      - name: PROMPT_VERSION
        value: "v1.0.0"

    resources:
      cpu: "500m"
      memory: "512Mi"

    healthcheck:
      path: /healthz
      interval: 10s
      timeout: 5s

# Deployment History (newest first, updated by CI/CD):
# -------------------------------------------------------------
# | Timestamp | Commit SHA | Deployed By |
# -------------------------------------------------------------
# | 2026-06-08T18:23:01Z | 7493f410a8f2e6716b2e995b4713b939a378e22b | Pnathan2544 |

# -------------------------------------------------------------
